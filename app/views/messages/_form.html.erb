<%= form_with model: [conversation, message], class: "message-form", id: "new_message" do |form| %>
  <%= form.text_area :body, placeholder: "Type your message here...", rows: 3, required: true %>
  <%= form.submit "Send", class: "send-button" %>
<% end %>