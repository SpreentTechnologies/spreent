<%= turbo_stream.append "messages", partial: "messages/message", locals: { message: @message, current_user_for_view: current_user } %>
<%= turbo_stream.replace "new_message", partial: "messages/form", locals: { conversation: @conversation, message: Message.new } %>